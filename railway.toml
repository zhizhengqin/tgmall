# Railway 部署配置 — TG Mall

[build]
dockerfilePath = "Dockerfile.railway"

[deploy]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
