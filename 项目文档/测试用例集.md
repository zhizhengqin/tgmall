# 测试用例集

## 柬埔寨 Telegram Mini App 社交电商平台

> **文档版本**：V1.0  
> **编制日期**：2026 年 6 月 6 日  
> **测试框架**：Jest (后端) + Vitest (前端) + Playwright (E2E)  
> **对应文档**：测试计划.md · API接口文档.md · 数据库设计说明书.md

---

## 目录

- [一、后端单元测试用例](#一后端单元测试用例)
  - [1.1 订单服务](#11-订单服务)
  - [1.2 支付服务](#12-支付服务)
  - [1.3 Bot 通知服务](#13-bot-通知服务)
  - [1.4 管理员服务](#14-管理员服务)
  - [1.5 工具函数](#15-工具函数)
- [二、后端集成测试用例](#二后端集成测试用例)
- [三、前端测试用例](#三前端测试用例)
- [四、E2E 测试用例](#四e2e-测试用例)
- [五、安全测试用例](#五安全测试用例)
- [六、性能测试用例](#六性能测试用例)

---

## 一、后端单元测试用例

### 测试环境

```javascript
// 测试框架：Jest 29 + ESM
// Mock 工具：tests/helpers/mocks.js
// 运行命令：npm test -- --testPathPattern="tests/unit"
```

---

### 1.1 订单服务

#### 测试套件：`tests/unit/order-service.test.js`

---

**TC-O-001：正常下单（单商品，无优惠券）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `userId: "user-1"`, `items: [{ productId: "prod-1", quantity: 2, spec: {} }]`, `shippingAddressId: "addr-1"`, `paymentMethod: "khqr"` |
| **Mock 数据** | 商品：`{ id: "prod-1", nameKm: "ទឹកក្រូចដូង", priceUsd: 2.50, priceKhr: 10000, stock: 10, status: "active" }` |
| **预期输出** | `order.status === "pending_payment"`, `order.totalUsd === 5.00`, `order.totalKhr === 40000` |
| **验证点** | 商品库存从 10 减为 8 |

---

**TC-O-002：正常下单（多商品）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-1", quantity: 1 }, { productId: "prod-2", quantity: 3 }]` |
| **Mock 数据** | prod-1: `{ priceUsd: 10, priceKhr: 40000 }`, prod-2: `{ priceUsd: 5, priceKhr: 20000 }` |
| **预期输出** | `totalUsd === 25.00`, `totalKhr === 100000` |
| **验证点** | 两个商品库存分别扣减 |

---

**TC-O-003：库存不足拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-1", quantity: 100 }]` |
| **Mock 数据** | 商品 stock = 10 |
| **预期输出** | 抛出 `AppError('"ទឹកក្រូចដូង"库存不足，仅剩10件', 400, "INSUFFICIENT_STOCK")` |

---

**TC-O-004：商品已下架拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-4" }]` |
| **Mock 数据** | 商品 status = "inactive" |
| **预期输出** | 抛出 `AppError("商品已下架", 410, "PRODUCT_INACTIVE")` |

---

**TC-O-005：商品不存在拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-nonexistent" }]` |
| **预期输出** | 抛出 `AppError("商品不存在: prod-nonexistent", 404, "NOT_FOUND")` |

---

**TC-O-006：fix 优惠券正确应用**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-1", quantity: 2 }]`, `couponId: "coupon-1"` |
| **Mock 数据** | 商品 priceUsd = 10, 优惠券 type = "fixed", value = 5, minSpend = 15 |
| **预期输出** | `totalUsd === 15.00`（20 - 5）, `discountUsd === 5` |
| **验证点** | 优惠券状态从 unused 变为 used |

---

**TC-O-007：百分比优惠券正确应用**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-1", quantity: 1 }]`, `couponId: "coupon-2"` |
| **Mock 数据** | 商品 priceUsd = 100, 优惠券 type = "percentage", value = 10 |
| **预期输出** | `totalUsd === 90.00`, `discountUsd === 10.00` |

---

**TC-O-008：优惠券过期拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `couponId: "coupon-expired"` |
| **Mock 数据** | 优惠券 endDate = `new Date("2020-01-01")` |
| **预期输出** | 抛出 `AppError("优惠券已过期", 400, "COUPON_EXPIRED")` |

---

**TC-O-009：未达最低消费拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `items: [{ productId: "prod-1", quantity: 1 }]`, `couponId: "coupon-3"` |
| **Mock 数据** | 商品 priceUsd = 5, 优惠券 minSpend = 20 |
| **预期输出** | 抛出 `AppError("未达到最低消费 $20", 400, "COUPON_MIN_SPEND")` |

---

**TC-O-010：COD 订单状态为 paid**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `paymentMethod: "cod"` |
| **预期输出** | `order.status === "paid"`, `paymentTimeout === null` |

---

**TC-O-011：KHQR 订单设置 15 分钟支付超时**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `paymentMethod: "khqr"` |
| **预期输出** | `paymentTimeout ≈ Date.now() + 15 * 60 * 1000`（误差 < 1000ms） |

---

**TC-O-012：自动匹配最优优惠券（金额优先）**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `couponId: undefined`, 商品 totalUsd = 100 |
| **Mock 数据** | 券 A: fixed $10, 券 B: percentage 15%（= $15）|
| **预期输出** | 选择券 B（折扣更大），`discountUsd === 15` |

---

**TC-O-013：自动匹配最优优惠券（同金额 fixed 优先）**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 商品 totalUsd = 100 |
| **Mock 数据** | 券 A: fixed $10, 券 B: percentage 10%（= $10） |
| **预期输出** | 选择券 A（同金额 fixed 优先） |

---

**TC-O-014：价格分摊精度（最大余数法）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | items = `[{ unitPriceUsd: 33.33, qty: 1 }, { unitPriceUsd: 33.33, qty: 1 }, { unitPriceUsd: 33.34, qty: 1 }]`, totalUsd = 100, discountUsd = 10 |
| **预期输出** | `distributed[0] + distributed[1] + distributed[2] === 90.00`（精确相等，误差 = 0） |
| **附注** | 已有测试 ✓（`price-distribution.test.js`），含 100 次随机验证 |

---

**TC-O-015：并发下单拒绝重复提交**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 同 userId 两个请求几乎同时到达 |
| **预期输出** | 第一个成功 → 201，第二个拒绝 → 409 `DUPLICATE_ORDER` |

---

**TC-O-016：Redis 锁释放安全（不误删他人锁）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 请求 A 获取锁(token=A)，超时后请求 B 获取锁(token=B)，请求 A finally 尝试释放 |
| **预期输出** | 请求 A 不能释放请求 B 的锁（token 不匹配） |
| **附注** | 已有测试 ✓（`redis-lock.test.js`） |

---

**TC-O-017：取消订单恢复库存**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 取消 orderId |
| **Mock 数据** | 商品 stock 从 10 减到 8（下单时扣 2），优惠券 status = "used" |
| **预期输出** | 商品 stock 恢复为 10，优惠券 status = "unused" |

---

**TC-O-018：取消订单拒绝（非 pending_payment 状态）**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 取消一个 status = "paid" 的订单 |
| **预期输出** | 抛出 `AppError`（订单状态不允许取消） |

---

**TC-O-019：确认收货**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 确认 status = "shipped" 的订单 |
| **预期输出** | `order.status === "completed"`, `completedAt` 非空 |

---

**TC-O-020：查询用户订单列表（分页 + 筛选）**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `userId: "user-1"`, `status: "paid"`, `page: 1`, `limit: 10` |
| **预期输出** | 返回 `{ items: [...], meta: { total, page, limit } }` |

---

### 1.2 支付服务

#### 测试套件：`tests/unit/payment-service.test.js`

---

**TC-P-001：KHQR 生成返回二维码（模拟模式）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `orderNumber: "ORD-TEST-001"`, `amountUsd: 29.99`, `amountKhr: 120000` |
| **Mock 数据** | `PAYMENT_MOCK_MODE = true` 或 `NODE_ENV !== "production"` |
| **预期输出** | `qrImageUrl` 非空，`transactionId` 以 `MOCK-BAKONG-` 开头 |

---

**TC-P-002：已支付订单拒绝重复支付**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | order.paymentStatus = "success" |
| **预期输出** | 抛出 `AppError("订单已支付，无需重复支付", 400, "ORDER_ALREADY_PAID")` |

---

**TC-P-003：已取消订单拒绝支付**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | order.status = "cancelled" |
| **预期输出** | 抛出 `AppError("订单已取消", 400, "ORDER_CANCELLED")` |

---

**TC-P-004：非 pending_payment 状态拒绝支付**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | order.status = "shipped" |
| **预期输出** | 抛出 `AppError("订单状态不支持支付", 400, "ORDER_NOT_PAYABLE")` |

---

**TC-P-005：支付超时拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | order.paymentTimeout = `new Date("2020-01-01")` |
| **预期输出** | 抛出 `AppError("支付已超时，请重新下单", 400, "ORDER_CANCELLED")` |

---

**TC-P-006：支付方式不匹配拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | order.paymentMethod = "aba_pay", 调用 KHQR 支付 |
| **预期输出** | 抛出 `AppError("该订单未选择 KHQR 支付方式", 400, "VALIDATION_ERROR")` |

---

**TC-P-007：支付回调签名验证通过**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 有效 HMAC 签名 |
| **Mock 数据** | `BAKONG_WEBHOOK_SECRET = "test-secret"` |
| **预期输出** | `verifySignature` 返回 `true` |

---

**TC-P-008：支付回调签名验证失败**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 无效签名 `"fake-signature"` |
| **Mock 数据** | `BAKONG_WEBHOOK_SECRET = "test-secret"` |
| **预期输出** | 抛出 `AppError("签名验证失败", 401, "UNAUTHORIZED")` |

---

**TC-P-009：支付回调签名密钥未配置时拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `BAKONG_WEBHOOK_SECRET = ""` |
| **预期输出** | `verifySignature` 返回 `false` |
| **附注** | CSO #1 回归测试 |

---

**TC-P-010：支付回调幂等性（10 次重复）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 同一 `transactionId` 调用 10 次 |
| **预期输出** | 第 1 次 → `{ status: "processed" }`，第 2-10 次 → `{ status: "duplicate" }` |
| **附注** | 已有测试 ✓（`payment-idempotence.test.js`） |

---

**TC-P-011：支付回调订单不存在**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `orderNumber: "ORD-NONEXISTENT"` |
| **预期输出** | 抛出 `AppError("订单不存在", 404, "NOT_FOUND")` |

---

**TC-P-012：支付回调事务失败不设置幂等标记**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | Mock 事务抛异常 |
| **预期输出** | 幂等标记不存在（可重试），`paymentStatus` 不变 |

---

**TC-P-013：支付成功更新订单状态**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | callback `{ status: "success", transaction_id: "txn-1" }` |
| **预期输出** | `order.paymentStatus === "success"`, `order.status === "paid"`, `paidAt` 非空 |

---

**TC-P-014：支付成功增加商品销量**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 订单包含 item `{ productId: "prod-1", quantity: 2 }` |
| **预期输出** | 商品 `salesCount` 增加 2 |

---

**TC-P-015：支付失败不更新订单为 success**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | callback `{ status: "failed" }` |
| **预期输出** | `order.paymentStatus === "failed"`, `order.status` 不变 |

---

### 1.3 Bot 通知服务

#### 测试套件：`tests/unit/notification-service.test.js`

---

**TC-N-001：sendMessage 正常发送**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `telegramId: "123456"`, `text: "🛒 ការបញ្ជាទិញបានបង្កើត!"` |
| **Mock** | `global.fetch` 返回 `{ ok: true, result: { message_id: 456 } }` |
| **预期输出** | `{ ok: true, messageId: 456 }` |

---

**TC-N-002：BOT_TOKEN 未配置时跳过**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `BOT_TOKEN = ""` |
| **预期输出** | `{ ok: false, error: "BOT_TOKEN_NOT_CONFIGURED" }`，不调用 fetch |

---

**TC-N-003：telegramId 为空时跳过**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `telegramId: null` |
| **预期输出** | `{ ok: false, error: "MISSING_TELEGRAM_ID" }` |

---

**TC-N-004：消息超过 4096 字符截断**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `text: "x".repeat(5000)` |
| **预期输出** | 实际发送的 text.length === 4096 |

---

**TC-N-005：用户输入 HTML 转义**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | 订单号包含 `<script>alert(1)</script>` |
| **预期输出** | 消息中不包含裸 `<script>` 标签，转义为 `&lt;script&gt;` |
| **附注** | 已有测试 ✓（`telegram-escape.test.js`） |

---

**TC-N-006：频率限制生效**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 同一用户同一类型通知 60 秒内第 2 次调用 |
| **预期输出** | 第 2 次返回 `{ ok: false, error: "RATE_LIMITED" }` |

---

**TC-N-007：新订单通知不受频率限制**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 连续 10 个新订单通知给同一管理员 |
| **预期输出** | 10 条全部发送成功 |

---

**TC-N-008：失败重试 3 次后放弃**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `fetch` 连续返回 `{ ok: false }` |
| **预期输出** | 重试 3 次后 status = "failed", retryCount = 3 |

---

**TC-N-009：通知记录入库**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 成功发送一条通知 |
| **预期输出** | `notifications` 表新增记录：`type: "user_created"`, `status: "sent"` |

---

**TC-N-010：三语模板按语言渲染**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `languageCode: "en"` |
| **预期输出** | 消息内容为英文 "Order Created!" |

---

**TC-N-011：未知语言回退高棉语**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `languageCode: "fr"` |
| **预期输出** | 消息内容为高棉语（默认回退） |

---

### 1.4 管理员服务

#### 测试套件：`tests/unit/admin-service.test.js`

---

**TC-A-001：大盘 GMV 统计**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **输入** | 查询大盘 |
| **预期输出** | `gmvToday ≥ 0`, `gmvThisMonth ≥ gmvToday`, `totalUsers ≥ 0` |

---

**TC-A-002：近 7 天趋势**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **输入** | 查询大盘 |
| **预期输出** | `recent7DaysTrend.length === 7`, 每天 `{ date, gmv, orders, newUsers }` |

---

**TC-A-003：用户列表搜索**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **输入** | `q: "សុភាព"` |
| **预期输出** | 返回的结果 firstName 或 lastName 包含 "សុភាព" |

---

### 1.5 工具函数

#### 测试套件：`tests/unit/utils.test.js`

---

**TC-U-001：订单号生成唯一性**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 连续生成 1000 个订单号 |
| **预期输出** | 无重复，格式匹配 `ORD-\d{8}-[A-Z0-9]{6}` |

---

**TC-U-002：JWT 签发与验证**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `payload = { userId: "user-1", role: "user" }` |
| **预期输出** | `verifyToken(signToken(payload)).userId === "user-1"` |

---

**TC-U-003：JWT 过期拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 过期的 JWT Token |
| **预期输出** | `verifyToken` 抛出异常 |

---

**TC-U-004：JWT 篡改拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | 修改 payload 但不更新签名 |
| **预期输出** | `verifyToken` 抛出异常 |

---

**TC-U-005：HTML 转义 — 用户输入**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **输入** | `"<script>alert('xss')</script>"` |
| **预期输出** | `"&lt;script&gt;alert('xss')&lt;/script&gt;"` |
| **附注** | 已有测试 ✓ |

---

**TC-U-006：HTML 转义 — null/undefined 安全**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **输入** | `null`, `undefined` |
| **预期输出** | `""`（空字符串，不崩溃） |

---

## 二、后端集成测试用例

### 测试环境

```bash
# 需要运行中的 PostgreSQL + Redis
docker compose up -d postgres redis
cd tgmall-api && npx prisma migrate deploy && npx prisma db seed
npm run test:integration
```

---

**TC-I-001：健康检查**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **请求** | `GET /api/v1/health` |
| **预期响应** | `200 { status: "ok", timestamp: "<ISO8601>" }` |

---

**TC-I-002：Telegram 登录 — 有效 initData**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **请求** | `POST /api/v1/auth/telegram` `{ init_data: "<valid>" }` |
| **预期响应** | `200 { success: true, data: { token: "<JWT>", user: {...} } }` |
| **验证点** | JWT payload 包含 `{ userId, telegramId, role: "user" }` |

---

**TC-I-003：Telegram 登录 — 无效 initData**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **请求** | `POST /api/v1/auth/telegram` `{ init_data: "hash=invalid" }` |
| **预期响应** | `401 { error: { code: "INVALID_INIT_DATA" } }` |

---

**TC-I-004：商品列表 — 无 Token**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **请求** | `GET /api/v1/products`（无 Authorization 头） |
| **预期响应** | 分页商品列表（公开接口，允许无 Token） |

---

**TC-I-005：商品详情**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **请求** | `GET /api/v1/products/:id` |
| **预期响应** | `200 { success: true, data: { id, nameKm, priceUsd, ... } }` |

---

**TC-I-006：商品详情 — 不存在的ID**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **请求** | `GET /api/v1/products/nonexistent-uuid` |
| **预期响应** | `404 { error: { code: "NOT_FOUND" } }` |

---

**TC-I-007：下单 — 完整流程**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **前置** | 有 valid Token, 有效 addressId |
| **请求** | `POST /api/v1/orders` `{ items, shipping_address_id, payment_method: "cod" }` |
| **预期响应** | `201 { success: true, data: { orderNumber, totalUsd, status: "paid" } }` |
| **验证点** | 商品库存减少、Redis 购物车清空 |

---

**TC-I-008：下单 — 无 Token**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **请求** | `POST /api/v1/orders`（无 Authorization 头） |
| **预期响应** | `401 { error: { code: "UNAUTHORIZED" } }` |

---

**TC-I-009：支付回调 — 成功**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **请求** | `POST /api/v1/webhooks/payment` 有效 payload + 有效签名 |
| **预期响应** | `200 { success: true, data: { status: "processed" } }` |
| **验证点** | 订单状态 → paid, salesCount 递增 |

---

**TC-I-010：支付回调 — 重复（幂等）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **请求** | 同一 payload POST 两次 |
| **预期响应** | 第 1 次 → 200 processed，第 2 次 → 200 duplicate |

---

**TC-I-011：速率限制 — 超过 100 次/15min**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **请求** | 同一 IP 101 次请求 |
| **预期响应** | 第 101 次 → `429 { error: { code: "RATE_LIMIT" } }` |

---

**TC-I-012：下单 → 支付回调 → 状态变更**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **场景** | 下单 → 支付回调 |
| **预期响应** | 状态从 pending_payment → paid |

---

**TC-I-013：下单 → 支付回调 → 销量更新**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **场景** | 下单 → 支付回调 |
| **预期响应** | salesCount 递增 |

---

**TC-I-014：下单 → 支付回调 → Bot 通知**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **场景** | 下单 → 支付回调 |
| **预期响应** | notifications 表有记录 |

---

**TC-I-015：下单 → 取消 → 库存恢复**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **场景** | 下单 → 取消 |
| **预期响应** | stock 回滚到原值 |

---

**TC-I-016：下单 → 支付 → 发货 → 确认收货**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **场景** | 完整状态流转 |
| **预期响应** | pending_payment → paid → shipped → completed |

---

## 三、前端测试用例

### 测试环境

```bash
# 管理员后台
cd tgmall-admin && npm run test:unit

# 消费者端 Mini App
cd tgmall-miniapp && npm run test:unit
```

---

### 3.1 管理员后台组件测试

---

**TC-F-001：LoginPage — Token 为空时登录按钮不可用**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 渲染 LoginPage，不输入 Token，点击登录 |
| **预期** | API 未被调用，提示"请输入 Token" |

---

**TC-F-002：LoginPage — 有效 Token 登录成功**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 输入有效 JWT，点击登录 |
| **Mock** | `api.get('/admin/dashboard')` 返回 200 |
| **预期** | `localStorage` 设置 `admin_token`，路由跳转 `/dashboard` |

---

**TC-F-003：LoginPage — 无效 Token 显示错误**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 输入无效 Token，点击登录 |
| **Mock** | `api.get('/admin/dashboard')` 返回 401 |
| **预期** | 显示错误提示，留在 `/login` |

---

**TC-F-004：DashboardPage — 数据加载渲染统计卡片**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 访问 `/dashboard` |
| **Mock** | 看板 API 返回 `{ todayRevenueUsd: 1250, pendingOrders: 8, productCount: 42 }` |
| **预期** | 4 张 StatCard 分别显示 "$1250", "8", "42" 等，ECharts 图表渲染 |

---

**TC-F-005：DashboardPage — 空数据不崩溃**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 访问 `/dashboard` |
| **Mock** | 看板 API 返回所有字段为 0/null |
| **预期** | 页面不报错，显示 "$0", "0" |

---

**TC-F-006：StatCard — Props 渲染正确**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **操作** | `<StatCard title="测试" value="100" color="#ff0000" />` |
| **预期** | DOM 显示 "测试" 和 "100"，value 颜色为 #ff0000 |

---

**TC-F-007：ProductsPage — 商品列表加载**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 访问 `/products` |
| **Mock** | `getProducts({ page: 1 })` 返回 `{ data: [...], meta: { total: 50 } }` |
| **预期** | el-table 渲染商品行，分页器显示 "1/3" |

---

**TC-F-008：ProductsPage — 上下架切换**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 点击 el-switch |
| **Mock** | `toggleProduct(id)` 返回 200 |
| **预期** | `getProducts` 重新调用，列表刷新 |

---

**TC-F-009：ProductFormPage — 新建模式**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 访问 `/products/new` |
| **预期** | 表单为空，标题显示 "បន្ថែមទំនិញ"，保存调用 `createProduct` |

---

**TC-F-010：ProductFormPage — 编辑模式**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 访问 `/products/prod-1` |
| **Mock** | `getProductById("prod-1")` 返回现有商品数据 |
| **预期** | 表单预填商品名、价格、库存，保存调用 `updateProduct` |

---

**TC-F-011：OrdersPage — 状态筛选**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 点击 "已付款" Tab |
| **预期** | `getOrders({ status: "paid" })` 被调用，列表只显示 paid 订单 |

---

**TC-F-012：OrderDetailPage — 发货操作**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 访问 `/orders/order-1`（status = "paid"）→ 填写物流信息 → 确认发货 |
| **Mock** | `shipOrder` 返回 200 |
| **预期** | shipping 按钮 loading 态，成功后刷新订单详情，物流信息显示 |

---

**TC-F-013：UsersPage — 搜索**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 输入 "សុភាព" → 触发搜索 |
| **Mock** | `getAdminUsers({ q: "សុភាព" })` 被调用，列表更新 |

---

**TC-F-014：Sidebar — 路由高亮**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 访问 `/products` |
| **预期** | Sidebar 中 "ទំនិញ" 为 `is-active` 状态（`active-text-color: #c4932a`） |

---

**TC-F-015：TopBar — 语言切换**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 选择 "English" |
| **预期** | `localStorage.setItem("admin_lang", "en")`，页面 reload |

---

**TC-F-016：TopBar — 登出**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 点击 "ចាកចេញ" |
| **预期** | `localStorage.removeItem("admin_token")`，重定向 `/login` |

---

**TC-F-017：路由守卫 — 未登录重定向**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 未登录直接访问 `/dashboard` |
| **预期** | 重定向到 `/login` |

---

**TC-F-018：路由守卫 — 已登录放行**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | `localStorage` 设置 Token → 访问 `/dashboard` |
| **预期** | 页面正常渲染 |

---

### 3.2 消费者端组件测试

---

**TC-F-019：ProductCard — 价格显示、售罄态**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 渲染商品卡片（stock = 0） |
| **预期** | 显示 "Sold Out" 蒙层，双币种价格正常 |

---

**TC-F-020：LoadingSkeleton — 骨架屏渲染**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 渲染 `LoadingSkeleton` |
| **预期** | 存在 4 个以上 skeleton 元素 |

---

**TC-F-021：EmptyState — 三语文案**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 切换语言 km/en/zh，渲染 `EmptyState` |
| **预期** | 文案随语言变化，高棉语为默认 |

---

**TC-F-022：BottomNav — 高亮态**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 访问 `/cart` |
| **预期** | 购物车图标为 active 颜色（`#c4932a`） |

---

## 四、E2E 测试用例

### 测试环境

```bash
# 启动所有服务
docker compose up -d
cd tgmall-admin && npm run build
cd tgmall-miniapp && npm run build

# 运行 E2E
npx playwright test
```

---

**TC-E-001：消费者完整购物流程**

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 打开 Mini App 首页 | 商品列表渲染，双币种价格显示 |
| 2 | 点击商品卡片 | 跳转商品详情，显示名称/价格/库存 |
| 3 | 点击 "加入购物车" | Toast 提示成功，底部导航购物车数量 +1 |
| 4 | 点击底部导航 "购物车" | 显示购物车商品列表 |
| 5 | 点击 "结算" | 跳转结算页，显示收货地址 |
| 6 | 选择支付方式 "KHQR" | 选择框高亮 |
| 7 | 点击 "提交订单" | 跳转支付页面，显示 QR 码 |
| 8 | 模拟支付回调（等 3 秒） | 跳转支付结果页，显示成功 |
| 9 | 点击 "查看订单" | 跳转订单列表，订单状态为 "paid" |

---

**TC-E-002：管理员后台发货流程**

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 打开管理员后台登录页 | 显示 Token 输入框 |
| 2 | 输入管理员 Token → 登录 | 跳转看板，显示统计卡片 |
| 3 | 点击侧边栏 "订单" | 订单列表 Tab 渲染 |
| 4 | 筛选 "已付款" | 显示 paid 订单 |
| 5 | 点击订单进入详情 | 显示商品明细 + 收货地址 + 发货表单 |
| 6 | 填写物流信息 → 确认发货 | 发货表单消失，显示物流信息 |
| 7 | 返回看板 | 待发货数字减 1 |

---

## 五、安全测试用例

---

**TC-S-001：Webhook 验签 — 密钥未配置拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **来源** | CSO 审计 #1 |
| **操作** | 设置 `BAKONG_WEBHOOK_SECRET = ""`，发送有效 payload |
| **预期** | `verifySignature` 返回 `false`，回调返回 401 |

---

**TC-S-002：Webhook 验签 — 无效签名拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 发送 `signature: "fake-signature"` |
| **预期** | 返回 `401 { error: { code: "UNAUTHORIZED" } }` |

---

**TC-S-003：速率限制 — 全局**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **来源** | CSO 审计 #2 |
| **操作** | 同一 IP 15 分钟内发 101 次请求 |
| **预期** | 第 101 次返回 `429 { error: { code: "RATE_LIMIT" } }` |

---

**TC-S-004：Dockerfile 不含 .env**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **来源** | CSO 审计 #4 |
| **操作** | `grep "COPY .env" tgmall-api/Dockerfile` |
| **预期** | 无输出（行已删除） |

---

**TC-S-005：.dockerignore 屏蔽 .env**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | `grep "^.env" tgmall-api/.dockerignore` |
| **预期** | 包含 `.env` 行 |

---

**TC-S-006：A01 — 水平越权（用户订单）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | user-A 的 Token 访问 `GET /api/v1/orders/user-B-order-id` |
| **预期** | 返回 `404`（订单不属于 user-A，getOrderById 用 userId 过滤） |

---

**TC-S-007：A02 — JWT 篡改拒绝**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 修改 JWT payload 中 role 为 "admin"，保持签名不变 |
| **预期** | API 返回 `401 { error: { code: "UNAUTHORIZED" } }` |

---

**TC-S-008：A03 — SQL 注入防护**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | `GET /api/v1/products?q='; DROP TABLE users; --'` |
| **预期** | 返回 200，空结果（参数化查询，不执行 SQL） |

---

**TC-S-009：A03 — XSS 防护（Bot 消息）**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 创建包含 `<script>alert(1)</script>` 的订单号，触发 Bot 通知 |
| **预期** | Bot 消息中 `<script>` 被转义为 `&lt;script&gt;` |

---

**TC-S-010：A07 — Token 过期**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 使用过期 JWT 调用 API |
| **预期** | 返回 `401 { error: { code: "UNAUTHORIZED" } }` |

---

**TC-S-011：A07 — 支付回调无 Token（正常）**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 支付回调不携带 Bearer Token |
| **预期** | 200（webhook 路由不在 auth middleware 下，使用签名验证） |

---

**TC-S-012：A10 — 支付回调 URL 不可控**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **操作** | 检查支付回调 handler 中 URL 来源 |
| **预期** | 回调 URL 由配置/环境变量驱动，不包含用户输入 |

---

## 六、性能测试用例

---

**TC-PF-001：并发下单 — 50 用户抢 10 库存**

| 字段 | 值 |
|------|-----|
| **优先级** | P0 |
| **操作** | 50 个并发 `POST /api/v1/orders`，商品 stock = 10 |
| **预期** | 最多 10 个成功（201），其余因库存不足拒绝（400 `INSUFFICIENT_STOCK`），stock 最终 = 0 |

---

**TC-PF-002：并发下单 — 同用户 10 并发**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | 同 userId 10 个并发下单 |
| **预期** | 第 1 个成功（201），其余 409 `DUPLICATE_ORDER` |

---

**TC-PF-003：商品列表 API — 1000 RPS**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **操作** | `ab -n 1000 -c 50 GET /api/v1/products` |
| **预期** | P99 < 200ms, 0 错误 |

---

**TC-PF-004：看板 API — 100 RPS**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **操作** | `ab -n 100 -c 10 GET /api/v1/admin/dashboard` |
| **预期** | P99 < 500ms（含近7天趋势查询） |

---

**TC-PF-005：前端首屏加载 — 4G 网络**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | Lighthouse 审计 `tgmall-miniapp` |
| **预期** | FCP < 1.5s, LCP < 3s |

---

**TC-PF-006：前端首屏加载 — 3G 网络**

| 字段 | 值 |
|------|-----|
| **优先级** | P1 |
| **操作** | DevTools 3G throttling → 加载首页 |
| **预期** | 完全加载 < 5s |

---

**TC-PF-007：管理员后台 Bundle 大小**

| 字段 | 值 |
|------|-----|
| **优先级** | P2 |
| **操作** | `cd tgmall-admin && npm run build` |
| **预期** | `dist/assets/*.js` 总 gzip < 500KB |

---

## 附录 A：测试执行统计

| 模块 | 总用例数 | P0 | P1 | P2 | 已有 |
|------|----------|-----|-----|-----|------|
| 订单服务 | 20 | 11 | 9 | 0 | 12 |
| 支付服务 | 15 | 11 | 4 | 0 | 7 |
| Bot 通知 | 11 | 6 | 5 | 0 | 8 |
| 管理员服务 | 3 | 0 | 0 | 3 | 0 |
| 工具函数 | 6 | 2 | 4 | 0 | 6 |
| 后端集成 | 16 | 12 | 4 | 0 | 0 |
| 前端组件 | 22 | 10 | 11 | 1 | 0 |
| E2E | 2 | 2 | 0 | 0 | 0 |
| 安全 | 12 | 7 | 4 | 1 | 0 |
| 性能 | 7 | 1 | 3 | 3 | 0 |
| **合计** | **114** | **62** | **44** | **8** | **33 (已完成)** |

## 附录 B：柬埔寨测试数据

```javascript
// 高棉语测试用户
const KM_USERS = [
  { firstName: 'សុភាព', lastName: 'កុង', phone: '+85512345678', language: 'km' },
  { firstName: 'ដារ៉ា', lastName: 'សុខ', phone: '+85587654321', language: 'km' },
  { firstName: 'ម៉ាលី', lastName: 'ចាន់', phone: '+85599887766', language: 'km' },
];

// 三语商品名测试
const TRI_LANG_PRODUCT = {
  nameKm: 'ទឹកក្រូចដូង',
  nameEn: 'Coconut Water',
  nameZh: '椰子水',
};

// 双币种价格测试（USD : KHR ≈ 1 : 4000）
const DUAL_CURRENCY = {
  priceUsd: 12.50,
  priceKhr: 50000,
};
```

---

*测试用例集版本 V1.0 — 共计 114 条用例，覆盖 10 个模块*
