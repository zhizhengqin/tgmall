# 组件库 — 待设计

以下组件需要单独导出状态集合截图。当前所有组件状态已在各页面 HTML 中实现，可直接从页面截取。

| 文件名 | 组件 | 包含状态 |
|--------|------|----------|
| 商品卡片 | Product Card | 默认、促销标签、售罄、多规格 |
| 按钮-状态集合 | Button States | 默认、Hover、Active、禁用、加载中 |
| 输入框-状态集合 | Input States | 默认、Focus、错误、禁用、带图标 |
| 底部导航栏 | Bottom Tab Bar | 5 个 Tab（首页/分类/购物车/订单/我的）+ 购物车角标 |

**建议**: 从已完成的页面 HTML 中用截图工具提取各组件状态。
